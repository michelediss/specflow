{
  "name": "specflow-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest --config vitest.config.ts --run"
  },
  "devDependencies": {
    "vitest": "^1.5.0"
  }
}
