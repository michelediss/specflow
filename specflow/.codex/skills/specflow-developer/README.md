# Specflow Developer Skill

Daily usage:

1. Read `spec/SPEC.md` and `decisions/DECISIONS.md` for context.
2. Pick the relevant `T-xx` task.
3. Implement tests and the minimal code to satisfy the linked ACs.
4. Run `npm run dev:guard -- --mode=developer` and `npm test`.
